__pycache__/
*.pyc

# Training-run checkpoint dumps (multi-hundred-MB, regenerable by re-running
# the ladder scripts; best_policy/ holds the actual deliverable checkpoints).
# The loose runs/*.log files (train/eval history) ARE tracked -- small and
# they're the evidence trail for every number in RL_course_MIRTE.pdf.
runs/ppo_gimbal_*/
runs/ctrl_test/
MUJOCO_LOG.TXT

# TensorBoard logs
runs/*/events.out.tfevents*

# Cinematic render intermediates + audio (hundreds of MB, fully regenerable by
# re-running scratchpad/cine_render2.py, cine_fails.py, generalize.py, cine_edit2.py).
# The finished film mirte_rl_film_4k.mp4 IS tracked.
scratchpad/cine/
scratchpad/cine2/
*.wav
